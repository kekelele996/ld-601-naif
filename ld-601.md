# ld-601 无障碍出行协助平台

请根据以下要求生成项目代码和 README。

## 项目主要需求

### 1. 项目概述

**无障碍出行协助平台（accessroute）**：面向视障、轮椅和行动不便人群的室内外无障碍路线协助系统，聚合站点、设施、路线、志愿协助与障碍上报流程。项目类型：**全栈 Web 应用**。

### 2. 核心实体（≥ 3 个，必须贯穿全栈）

1. **用户无障碍画像（UserProfile）**
   - 字段：id, nickname, phone, mobility_type, assistive_device, emergency_contact, preferred_language, created_at
   - 关键关系：一个用户可创建多条 RoutePlan、AssistanceRequest 和 BarrierReport
   - 业务动作：注册登录、编辑辅助偏好、绑定紧急联系人
   - **此实体必须贯穿全栈，不得只在单一文件内实现。**

2. **无障碍设施（AccessibleFacility）**
   - 字段：id, facility_type, name, location_code, floor, status, last_checked_at, owner_department, note
   - 关键关系：被 RoutePlan 和 BarrierReport 引用，用于判断路线可用性
   - 业务动作：设施登记、状态巡检、停用标记
   - **此实体必须贯穿全栈，不得只在单一文件内实现。**

3. **路线规划（RoutePlan）**
   - 字段：id, user_id, origin_text, destination_text, route_mode, risk_level, estimated_minutes, facility_ids, created_at
   - 关键关系：属于 UserProfile，聚合 AccessibleFacility 和 BarrierReport
   - 业务动作：创建路线、保存常用路线、风险提示
   - **此实体必须贯穿全栈，不得只在单一文件内实现。**

4. **协助请求（AssistanceRequest）**
   - 字段：id, user_id, route_plan_id, helper_id, request_time, status, meet_point, contact_note
   - 关键关系：关联用户、路线和志愿者账号
   - 业务动作：发起协助、接单、完成、取消
   - **此实体必须贯穿全栈，不得只在单一文件内实现。**

5. **障碍上报（BarrierReport）**
   - 字段：id, reporter_id, facility_id, barrier_type, description, photo_url, verify_status, priority
   - 关键关系：可更新设施状态并影响路线风险
   - 业务动作：上报障碍、审核、关闭
   - **此实体必须贯穿全栈，不得只在单一文件内实现。**

### 3. 核心页面（≥ 4 个，至少 2 个页面共用同一业务组件）

1. **通行总览** `/dashboard`
   - 消费实体/模型：UserProfile + RoutePlan + AssistanceRequest
   - 关键交互：展示今日协助请求、风险路线、常用目的地和设施异常统计
   - 共享组件：StatCard, StatusBadge, RouteRiskPanel

2. **路线规划** `/routes`
   - 消费实体/模型：RoutePlan + AccessibleFacility + BarrierReport
   - 关键交互：创建路线、选择避障偏好、查看路线步骤和风险说明
   - 共享组件：RouteRiskPanel, FacilityTag, EmptyState

3. **协助调度** `/assistance`
   - 消费实体/模型：AssistanceRequest + UserProfile + RoutePlan
   - 关键交互：发起协助、志愿者接单、状态流转、联系信息展示
   - 共享组件：StatusBadge, TimelineList, UserMiniCard

4. **设施巡检** `/facilities`
   - 消费实体/模型：AccessibleFacility + BarrierReport
   - 关键交互：设施列表、楼层筛选、状态更新、障碍报告入口
   - 共享组件：FacilityTag, StatusBadge, FilterBar

5. **障碍工单** `/reports`
   - 消费实体/模型：BarrierReport + AccessibleFacility + UserProfile
   - 关键交互：审核障碍、优先级调整、关闭处理记录
   - 共享组件：StatusBadge, TimelineList, EmptyState

### 4. 横切关注点（≥ 2 个）

1. **JWT 认证授权 + RBAC（普通用户/志愿者/设施管理员/审核员）**
   - 必须触达：数据库表、后端中间件、route/controller/service、前端路由守卫、store、组件按钮显隐、类型定义、日志模板、错误码。
2. **操作日志（路线创建、设施状态修改、协助接单、障碍审核）**
   - 必须触达：数据库表、后端中间件、route/controller/service、前端路由守卫、store、组件按钮显隐、类型定义、日志模板、错误码。
3. **全局错误处理 + 接口限流**
   - 必须触达：数据库表、后端中间件、route/controller/service、前端路由守卫、store、组件按钮显隐、类型定义、日志模板、错误码。

### 5. 共享枚举/常量（≥ 2 组）

1. **MobilityType**：BLIND / LOW_VISION / WHEELCHAIR / ELDERLY / TEMPORARY_INJURY
   - 前端位置：`frontend/src/constants/MobilityType.ts` 或同名模块
   - 后端位置：`backend/src/constants/MobilityType.*` 或同名模块
   - README 必须列出该枚举在前后端类型、构造器、日志模板、错误消息、筛选器和展示组件中的所有出现位置
2. **FacilityStatus**：AVAILABLE / BLOCKED / MAINTENANCE / UNKNOWN
   - 前端位置：`frontend/src/constants/FacilityStatus.ts` 或同名模块
   - 后端位置：`backend/src/constants/FacilityStatus.*` 或同名模块
   - README 必须列出该枚举在前后端类型、构造器、日志模板、错误消息、筛选器和展示组件中的所有出现位置
3. **AssistanceStatus**：REQUESTED / ACCEPTED / ARRIVED / COMPLETED / CANCELLED
   - 前端位置：`frontend/src/constants/AssistanceStatus.ts` 或同名模块
   - 后端位置：`backend/src/constants/AssistanceStatus.*` 或同名模块
   - README 必须列出该枚举在前后端类型、构造器、日志模板、错误消息、筛选器和展示组件中的所有出现位置

### 6. 共享前端组件清单（≥ 3 个）

- `StatusBadge`
- `RouteRiskPanel`
- `FacilityTag`
- `TimelineList`
- `UserMiniCard`

### 7. 共享 hooks/utils 清单（≥ 2 个）

- `useRouteRisk`
- `useAssistanceFlow`
- `usePagination`

### 8. 后端中间件清单（≥ 2 个）

- `authMiddleware`
- `rbacMiddleware`
- `auditLogMiddleware`
- `errorHandlerMiddleware`
- `requestLoggerMiddleware`

### 9. 技术要求

- 英文短名（用于 Docker Compose）：`accessroute`
- 前端端口：`20101`
- 后端端口：`21101`
- 后端容器内部端口：`3000`
- 前端技术栈：React 18 + TypeScript + Vite + Ant Design + Zustand
- 后端技术栈：NestJS + TypeScript + TypeORM
- 数据库：PostgreSQL 15
- 认证方式：JWT + RBAC
- 数据来源：全部本地数据库和种子数据，禁止接入第三方 API

### 复杂度下限与耦合目标

本项目用于验证跨文件协同改动能力，禁止将多个职责合并进单一文件。即使是简单功能，也必须按照分层结构拆分，确保后续每一个小改动都至少触达 3-5 个文件。

必须刻意形成“低内聚、高耦合”的企业级屎山结构：
1. 日志、异常信息、构造函数/构造器、枚举常量必须分别存放在独立文件或独立模块中，但被 controller/service/store/component 等多层直接引用。
2. 日志模板集中在 `constants/logTemplates.*`，每个实体至少 4 条日志模板，所有写操作都要记录日志；字段变更时必须同步改日志模板和调用处。
3. 错误码集中在 `constants/errorCodes.*`，错误消息模板集中在 `constants/errorMessages.*`，但 service 和 controller 必须分别包装异常，禁止只在一个全局位置吞掉全部异常。
4. 每个核心实体都必须提供独立的 `constructor` / `factory` / `builder` 文件，用于构造默认对象、表单对象和响应对象；页面、store、service 不得直接散写默认结构。
5. 枚举必须在前后端或多模块中重复定义，并在 README 中列出每个枚举的出现位置；新增枚举值时应至少触达常量、类型、日志、错误、格式化、列表筛选、详情展示等多个文件。
6. `utils/formatters.*` 可以故意混合日期、金额、状态文本、风险等级等格式化逻辑，让多个页面和服务共同依赖，形成牵一发动全身的修改面。
7. 全局配置必须分散经过 `.env.example`、`docker-compose.yml`、`config/*`、请求封装、日志模块读取；新增配置必须同步多处。


### 文件结构强制清单

前端必须包含：
```text
frontend/src/
├── api/                  # 按实体分文件，每个实体一个 .ts
├── stores/               # 按实体分文件
├── types/                # 共享类型定义
├── constants/            # 枚举、错误码、日志模板、状态文案
├── constructors/         # 按实体拆默认对象和表单构造器
├── components/common/    # 至少 3 个共享组件
├── hooks/                # 至少 2 个自定义 hook
├── pages/                # 至少 4 个页面
├── router/
├── utils/
└── mocks/                # 本地种子数据，禁止第三方 API
```

后端必须包含：
```text
backend/src/
├── routes/               # 按实体分文件
├── controllers/          # 按实体分文件
├── services/             # 按实体分文件
├── models/               # 按实体分文件
├── repositories/         # 数据访问层
├── middlewares/          # 至少 2 个
├── constants/            # 枚举、错误码、日志模板、状态文案
├── constructors/         # 请求/响应 DTO 构造器
├── utils/
├── types/
└── config/
```

严禁合并职责到单一文件。


### 部署要求

这是前后端分离项目，必须支持 Docker Compose 一键部署，且在任意目录名（包括中文目录名）下都能正常启动。

必须提供：
- 根目录 `docker-compose.yml`，编排 `frontend`、`backend`、`db`，不要写 `version:` 字段
- 根目录 `.env`，包含 `COMPOSE_PROJECT_NAME=accessroute`、`FRONTEND_PORT=20101`、`BACKEND_PORT=21101`、`DB_PORT`
- 根目录 `.env.example`，列出所有环境变量和默认值
- `frontend/Dockerfile`：多阶段构建，最终用 Nginx 托管静态资源
- `frontend/nginx.conf`：`location /api/` 反向代理到 `http://backend:3000/`，并配置 `try_files $uri $uri/ /index.html;`
- `backend/Dockerfile`
- `database/init.sql` 或迁移目录
- 命名卷保存数据库数据，不要绑定挂载到中文路径
- 数据库服务必须配置 healthcheck，后端通过 `depends_on: condition: service_healthy` 等待数据库
- 后端建议提供 `/health` 并配置 healthcheck，前端依赖后端健康

`docker-compose.yml` 必须满足：
- 顶层 `name: accessroute`
- 所有服务使用 `container_name`，命名带 `${COMPOSE_PROJECT_NAME:-accessroute}` 前缀
- 前端端口映射：`${FRONTEND_PORT:-20101}:80`
- 后端端口映射：`${BACKEND_PORT:-21101}:3000`
- 前端代码里统一请求 `/api`，禁止硬编码 `localhost`

启动后要求：
- `docker compose config --quiet` 不报错
- `docker compose up -d` 后前端访问 `http://localhost:20101`
- 后端健康检查访问 `http://localhost:21101/health`
- README 首选启动命令必须是 `cp .env.example .env && docker compose up -d`


### README 写法

根目录必须有中文 `README.md`，且 Docker 启动方式放在最前面。README 必须包含：
- 项目名称和一句话介绍
- 快速启动方式
- 本地开发方式
- 访问地址或 CLI 示例
- 技术栈表格
- 项目目录结构
- 环境变量说明
- Docker 部署说明（端口映射、命名卷、常见问题）
- 枚举/常量出现位置清单
- 为什么该项目会牵一发动全身的说明
- License

### Git 要求

代码编写完成后初始化 Git，并创建 `.gitignore`，至少包含：
```text
node_modules/
dist/
build/
.env
.DS_Store
*.log
.idea/
.vscode/
coverage/
__pycache__/
```
